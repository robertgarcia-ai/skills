> **SANITIZED EXEMPLAR.** Personal names, usernames and machine-specific identifiers were replaced (Operator, REDACTED-USER, ExampleCorp, Sample), the app's identity was additionally fictionalized (SampleApp / sample-app / Nudge Queue and kindred renames), and timezone and personal context were generalized; everything else — structure, numbers, wording — is verbatim from a completed real campaign (2026-07). Read for shape and conventions, not as instructions to execute.
>
> **Take from this:** the one-paragraph methodology preamble with raw→merged counts and the two-verifier rule; consequence-first finding titles; Repro lines written to be executable as a fixer's test case; the Disputed section carrying the refuting verifier's full mechanism; "Nothing has been fixed." Map: preamble · totals · Confirmed (high/med/low) · Disputed · Refuted.

# Bug hunt — 2026-07-23

Full-codebase audit of `src/` by a 12-agent review fleet (7 subsystem auditors + 5 cross-cutting lenses: dates/timezones, filesystem races, IPC contract drift, legacy-data tolerance, model-API usage). 146 raw findings were merged to 90 unique; every finding was then attacked by two independent adversarial verifiers (one checks the code mechanism, one walks the user repro end-to-end). **Confirmed** = both verifiers upheld it. **Disputed** = split verdict. Nothing has been fixed.

Totals: **78 confirmed** (17 high / 30 medium / 31 low), **5 disputed**, **7 refuted**.

## Confirmed — high

### src/main/index.ts:659 — Quit during an in-flight watched-folder ingest strands the claimed file in _captured/ with no chat (silent capture loss)

pollCaptureFolder claims each file by renameSync into _captured/ before the async ingest (transcribe API call, then createChat). Nothing protects an in-flight poll at quit: in the default interval mode before-quit does nothing and app.quit() exits mid-ingest; in close-scan modes (-1/-2) the before-quit scan returns immediately if capturePolling is already true (line 628) — skipping protection exactly when a poll is in flight — and the 15s Promise.race cap (line 1451) resolves mid-transcription and quits anyway, also skipping the scan of freshly dropped files. The file sits in _captured/, which the scanner permanently skips (leading '_'), so it is never retried.

**Repro:** User has the default 30s watch interval; a photo note syncs into the capture folder; the poll renames it into _captured/ and starts vision transcription; user closes the app → process exits before createChat. Next launch: the photo is in _captured/ (skipped forever), no chat in IN, no log entry — the capture silently never happened. Same loss in on-close mode when transcription exceeds 15s or a poll was already running.

### src/main/index.ts:664 — Capture-folder ingest failure (offline/API error) permanently drops the capture — file already moved to _captured/, never retried

The file is renamed into _captured/ first (lines 657-661); if ingestCapture then throws (transcribeCapture fails: offline, invalid key, rate limit), the catch at line 664-672 only appends a triage-log line. The file is never moved back into the scan zone and _-prefixed names are skipped (line 642), so the capture is never retried and no chat is ever created. Contrast the rename-failure path (line 660-662), which is deliberately retried next tick.

**Repro:** User's laptop is offline; three photo notes land in the watched folder; each is moved to _captured/ and transcription throws. Back online, nothing is ever ingested: the photos never appear in IN and the folder looks processed; only a buried Activity Log line mentions the failure.

### src/main/index.ts:463 — Tickler-parked files of unsupported types (PDF/DOCX/HEIC/etc.) are ingested as UTF-8 mojibake and the original file is permanently DELETED

The tickler dropbox accepts any file type (App.tsx dropbox has no accept filter; saveTicklerFiles writes anything). ingestTicklerFile maps the extension via CAPTURE_EXT[ext] ?? '' (line 463) — any non-image/text extension falls to mime '' and ingestCapture's text branch does buf.toString('utf8') (line 560), producing binary garbage as chat content, then triages the noise. On success deleteTicklerFile (line 472) rmSync's the original — no trash, no copy (the userData/captures backup only happens for recognized image mimes). The watched-folder path filters extensions (line 644); the tickler and drag-drop paths do not.

**Repro:** User drags concert-tickets.pdf (or an iPhone .heic) onto a Nudge Queue day. On the due date they click 'Triage this date': the file is decoded as UTF-8 garbage into a chat and deleteTicklerFile removes the PDF — the only copy of the user's file is unrecoverably gone; all that remains is a mojibake chat.

### src/main/index.ts:385 — Parked tickler FILES silently roll forward a whole year if their due day is missed — and flip to next year on the due-day evening for west-of-UTC users

ticklerFileCounts maps every nudge_queue day folder through nextOccurrenceISO (lines 256-261), which by construction never returns a past date, and dueTicklerDates (411-414) filters iso <= today — so a file-only date is 'due' ONLY on the exact day itself. Once that day passes (or, because the comparison uses the UTC date, once UTC midnight passes on the due day — ~4-8pm local for the Americas), the folder remaps to NEXT YEAR and vanishes from the due set and the TNON nag. Parked chats don't have this problem (their DB folder keeps a fixed past ticklerDue and nags forever).

**Repro:** User parks a file for July 20 and doesn't open the app that day. Opening it July 21, no nag and nothing due — the file has silently remapped to July 20 of NEXT YEAR. A US user hits the same loss on the correct day if they only open the app after ~8pm local.

### src/main/index.ts:1357 — items:set-status 'done' awaits triageChat with no error handling — completing an item while offline rejects the IPC after the DB mutation, so the UI never refreshes

The handler persists setItemStatus(p.id,'done') first, then `await triageChat(item.chatId, item.projectId)` un-guarded. triageChat calls the Anthropic API and throws on no key/no network/rate limit; the rejection propagates out of ipcMain.handle, so the renderer's await in itemAction (App.tsx:701) throws before loadItemsFor runs; completeItem (App.tsx:732) has try/finally but no catch. proposeForProject in the same handler IS wrapped in try/catch ('a failed proposal must not break completion') — the same protection is missing for the re-triage.

**Repro:** User is offline and clicks Done on a chat-extracted item. The throbber spins, then nothing — the item stays visible with no error, but the DB marked it done, so it silently vanishes on the next navigation; re-clicking Done fails the same way.

### src/main/index.ts:1195 — Calendar export reports events it silently dropped, and user-created events can never be given a date or exported at all

calendar:export returns count: events.length, but buildICS (ics.ts:76-78) silently skips any item whose data lacks a parseable date — every user-created event (createUserItem stores data:'', store.ts:242) and any legacy/corrupt row. With only dateless events it still writes a VCALENDAR with zero VEVENTs and the renderer announces success. There is also no UI to give a hand-added event a date: the ⋯ 'Deadline' editor writes `deadline`, which eventDetails ignores (items.ts:45), so such events get no When sort key, no per-event 'Add to Google Calendar'/'Export .ics' entries (gated on eventOf, App.tsx:3476), and are permanently un-exportable while inflating the reported count.

**Repro:** User has two triaged events plus one typed by hand via '+ Add an item' in Events. Export calendar reports 'Exported 3 events' but the .ics contains only 2 — the hand-added event is silently missing, and there is no way to ever get it into any export.

### src/main/store.ts:160 — Deleting a parked chat strands its items invisibly forever in the tickler folder

deleteChat (157-162) soft-deletes the chat and sets its items' chat_id='' but leaves project_id pointing at the tickler folder. NOT_IN_TICKLER (213-214) hides those items from listItems/listWaiting; the folder itself disappears once it holds zero chats (listTicklerFolders requires chatCountFor > 0), so it drops from the calendar and due-nag. triageTicklerDate only resurfaces items by moving CHATS (moveChat matches by chat_id, now ''), so nothing ever moves these items out, and no UI path queries items by tickler project_id.

**Repro:** User parks a chat with confirmed active items, then from the day view uses '⋯ → Delete chat'. The chat disappears, but its confirmed items — which the code comment promises will 'survive but become unclaimed' — never reappear in any list. Permanently invisible, no recovery path.

### src/main/store.ts:360 — replaceChatSuggestions hard-deletes ALL still-suggested items of the chat on every re-triage — wiping user edits and pulling items back out of make-project projects

replaceChatSuggestions starts with DELETE FROM items WHERE chat_id = ? AND status = 'suggested' — scoped only by chat_id — and re-inserts fresh model drafts with new ids. Two consequences: (1) user edits to suggested items (rename, notes, deadlines, buy-link/location pins — all UI-supported) live on the deleted row and are silently lost; trigger paths are routine (completing any sibling item auto-re-triages at index.ts:1357, 'Triage Anew' forces re-triage, 'Triage changed chats'). (2) items:make-project (index.ts:1389-1397) moves a candidate's suggested next actions into the new project without moving the source chat; the next triage of that chat hard-deletes those items and re-inserts them under the CHAT's project — the new project's steps vanish.

**Repro:** User adds a location pin and fixes the title on a suggested buy item, then marks a sibling done: the pin, rename and deadline silently disappear, replaced by the model's original draft. Separately, after 'Make project', continuing to chat in the source chat makes the project's filed steps vanish and reappear as suggestions in the old location.

### src/main/store.ts:67 — persist() rewrites the entire database in place non-atomically after every mutation — crash mid-write corrupts sample.sqlite with no backup

persist() is writeFileSync(dbPath, Buffer.from(db.export())) — a full truncate-and-rewrite of the only copy of all user data, run synchronously after EVERY mutation (every message, every status click), with no write-to-temp-then-rename and no backup. The file grows with chat history, so the vulnerable window widens. A crash, power loss, force-kill, or disk-full mid-write leaves a truncated/garbage file that new SQL.Database(readFileSync(dbPath)) cannot load.

**Repro:** Power is cut during any of the constant whole-file rewrites. Next launch: the DB fails to load and the user's entire task history — chats, items, projects — is unrecoverable.

### src/main/store.ts:30 — Corrupt/truncated sample.sqlite makes the app launch to nothing — no window, no error message, zombie process

initStore loads the DB with new SQL.Database(readFileSync(dbPath)) and runs DDL; a corrupt file throws. The caller app.whenReady().then(async () => { await initStore(); … createWindow(); }) (index.ts:1408) has no catch: the rejection is an unhandled promise rejection, createWindow never runs, and the process sits headless — no window, no dialog, no quit path except the task manager. Corruption is realistic given persist()'s non-atomic full rewrites.

**Repro:** Machine loses power mid-persist, truncating sample.sqlite. On next launch the app appears to do nothing: no window, no error, a zombie process — on every launch until the file is manually deleted (losing all data).

### src/renderer/src/App.tsx:350 — File drop outside the dropbox on a Nudge Queue day navigates the whole app to the dropped file

While a Nudge Queue day is open, captureSuppressedRef.current is true (line 311), so hasFiles() returns false and none of the window-level dragover/drop handlers call preventDefault (349-373); only the small day-view dropbox (1306-1321) prevents default. Chromium's default for an unhandled file drop is to navigate to the file:// URL, and main's will-navigate guard (index.ts:981-985) only intercepts /^https?:\/\// URLs, letting file:// through.

**Repro:** User opens a Nudge Queue day, drags a file toward the '📎 Drop files here' box but releases slightly outside it. The entire Electron window navigates to the dropped file — the app UI is replaced by raw file contents and the user must restart the app.

### src/renderer/src/App.tsx:1096 — Renderer Nudge Queue targets and Park dialog use UTC 'today' — evening users west of UTC park chats a full year out and can't select today

nextOccurrenceISO (1092-1097) compares the local-calendar iso(month,day) against now.toISOString().slice(0,10) (UTC). West of UTC after ~4-8pm local, UTC today is already tomorrow, so the local-today cell compares as past and returns NEXT YEAR's date; renderNudgeMonth uses this as the open/park/drop target (lines 1223, 1239). The 'Park for later…' dialog's default parkDate (line 1818) and min attribute (line 2479) use the same UTC value, so the picker defaults to tomorrow and refuses today in the evening (and east of UTC allows parking to local yesterday). Main shares the UTC convention, so nothing corrects it downstream — snapTicklerISO keeps the year-out date.

**Repro:** At 8pm EDT a user drags a chat onto today's cell in the month grid (or clicks today and drops files). The content is silently parked for the same date NEXT YEAR — it vanishes from IN, no nag fires, and it sleeps ~365 days. Opening 'Park for later…' at 9pm, today is below min and unselectable.

### src/renderer/src/App.tsx:874 — Project filter set in a smart list silently filters the IN and WEEKLYSWEEP views, which have no filter control — unfiled proposals all hidden

displayItems (872-877) applies listProjFilter in EVERY section, keeping items only if `it.projectId && listProjFilter.has(it.projectId)` — dropping every unfiled item (projectId null). The filter picker renders only when SECTION_KIND[section] is truthy (line 2917: next/buy/calls/events); 'in' and 'weeklysweep' have no entry, yet the filter still applies there with no indicator, and it is never reset on navigation. IN items mostly have projectId null, so ALL of them are hidden; IN still shows the 'Needs your input' header over zero rows, and a fully filtered smart list renders neither rows nor its empty-state text.

**Repro:** User filters Next actions to one project via the Projects pill, then clicks IN. Every proposed item, project candidate and park suggestion is invisible, with no clue why and no way to clear the filter from that view — pending triage proposals are missed entirely. WEEKLYSWEEP similarly hides waiting items.

### src/renderer/src/App.tsx:325 — File ▸ 'Triage changed chats' menu handler is a stale mount-time closure: it blanks the currently viewed list and its re-entrancy guard never works

The menu-listener effect runs once with [] deps (322-339), so onMenuTriage captures the FIRST render's doTriageAll: `section` is permanently 'home' and `triaging` permanently false. On completion, loadItemsFor('home') hits the !kind fallthrough (567-571) and calls setItems([]) — wiping whatever list the user is viewing and never refreshing it (if viewing a project, the stale projectId=null likewise blanks projItems). The stale triaging guard (line 668) also lets repeated menu clicks start overlapping concurrent triage runs (duplicate model calls, interleaved replaceChatSuggestions writes).

**Repro:** User views Next actions with 10 items and picks File ▸ Triage changed chats. When triage finishes, the list goes completely blank (new suggestions not shown) until they navigate away and back; picking the menu item again mid-run silently starts a second concurrent triage.

### src/renderer/src/App.tsx:3977 — Pressing Enter in the composer while the chat is generating silently discards the typed message

Composer's Enter keydown (3977-3980) calls submit() with no busy guard; submit() (3923-3928) calls onSend(text) and then unconditionally setValue(''). useChat.send (useChat.ts:119) early-returns when the active chat is in busyChats with no feedback. Only the Send button is swapped for Stop while busy — Enter still submits, so the guard path is reachable from normal typing.

**Repro:** While Claude streams a reply, the user types a follow-up and presses Enter. The composer clears as if sent, but the message was never sent or queued — the text is lost with no feedback and must be retyped.

### src/renderer/src/App.tsx:3524 — 'Move to IN' on a confirmed (active) or waiting item makes it permanently invisible in every view

The ⋯ menu offers MOVE_TARGETS including inbox/'IN' for items of any status (3524), and moveItem (843) patches only `kind`, leaving status 'active'/'waiting'. No query ever lists kind='inbox' with those statuses: the IN view loads only suggested (+dismissed/done toggles, 550-565), smart lists filter by their own kind, listWaiting (store.ts:229) and listItemsForProjects (store.ts:344) restrict to next/buy/call. The row survives in the DB but is unreachable from the UI with no restore path.

**Repro:** User confirms a Next action, then picks '⋯ → Move to IN' thinking it re-inboxes it. The item vanishes from Next actions, never appears in IN even with all toggles on, and is gone from every list — perceived permanent data loss.

### src/provider/anthropic.ts:186 — Triage prompt feeds the model the UTC date as 'Today's date', so evening triage resolves relative dates and deadlines one day late

The triage user message is built with `Today's date: ${new Date().toISOString().slice(0,10)}` — the UTC calendar date. For a US user, every triage run between ~7-8 PM and midnight local tells the model today is tomorrow. The schema instructs resolving relative dates ('by Friday', 'tomorrow') against this date (schema.ts:22, 76), so all deadlines, event dates, and nudgeQueue park dates extracted from evening captures shift one day forward.

**Repro:** At 9 PM Tuesday Eastern the user captures 'call the dentist tomorrow'. The model is told today is Wednesday, so it resolves 'tomorrow' to Thursday — no urgency badge appears Wednesday and the call is missed.

## Confirmed — medium

### src/main/index.ts:260 — All main-process tickler due/park date math uses the UTC calendar day, so dates come due hours early (or late) and mix local/UTC years around New Year

dueTicklerDates (411-414), normalizeNudgeDate (271), triageChat's nqFuture check (177), and store.todayISO (store.ts:681, used by dueTicklerFolders) all derive 'today' from new Date().toISOString().slice(0,10) — the UTC date — while the tickler is a local-calendar concept. nextOccurrenceISO (256-261) additionally mixes LOCAL now.getFullYear() with the UTC today string, so around New Year (Dec 31 evening west of UTC / Jan 1 morning east) a due-today folder maps a full year off. Consequences: west-of-UTC users see parked dates become due the prior local evening (TNON nag, due highlighting, File menu); an evening model-proposed 'park for tomorrow' fails nqDate > today and the park routing is discarded; east-of-UTC users see due dates hours late. Inconsistent with snapTicklerISO (store.ts:727), which parses in LOCAL time.

**Repro:** A US Central user parks a chat for July 23. At 7:01pm on July 22 (00:01 July 23 UTC), 'Today Needs Occasional Nudging!' appears and the July 23 folder shows as due — the evening before the chosen day; triaging it delivers the chat back to IN a day early.

### src/main/index.ts:178 — Model auto-park on re-triage silently hides the user's CONFIRMED active items until the park date — including after completing a sibling item, while the UI says 'nothing new surfaced'

triageChat's confident nudge-queue branch (lines 176-189) calls parkChat, which via moveChat (store.ts:126-137, 742-746) moves the chat AND every item bound to it — including confirmed active items — into the tickler folder, where NOT_IN_TICKLER (store.ts:213-214) hides them from listItems/listWaiting and the plan-lists export. This fires without approval whenever a changed chat is re-triaged, including the items:set-status 'done' completion re-triage (line 1357); in that path the parked result returns {added:0, parked:1} and the renderer (App.tsx:708-714) reads only retriaged.added, showing 'Re-triaged after completing — nothing new surfaced.' with no mention of the park.

**Repro:** A concert chat has confirmed items 'Buy tickets' and 'Book hotel'. The user marks 'Buy tickets' done; the re-triage confidently parks the chat for the concert date — 'Book hotel' instantly disappears from every list and export until that date, while the status line claims nothing changed.

### src/main/index.ts:874 — triage:all-changed processes tickler-parked chats: a manually parked chat gets triaged early and can be silently re-parked to a model-chosen date

allChatsForTriage (store.ts:120) has no filter for chats in location='tickler' folders, and the tickler:park handler (line 1172) does not set the chat's triaged hash (unlike the auto-park path at line 181). A manually parked chat with hash !== triagedHash is picked up by the next triage-all run with parkedFor undefined: a confident future nudgeQueue date re-parks it to the MODEL's date, overriding the user's explicit park date; otherwise suggestions are filed with the tickler folder as project.

**Repro:** User writes 'renew the domain — it expires March 12' and manually parks it for November 3. Clicking 'Triage changed chats' triages the parked chat immediately and re-parks it into the March 12 folder — the chosen November 3 date is silently discarded.

### src/main/index.ts:571 — ingestCapture is non-atomic: an auto-triage failure after the chat is created is reported as a capture failure — inviting duplicate re-capture across drag-drop, watcher, and tickler paths

ingestCapture creates the chat (line 564-565) and THEN awaits triageChat un-caught (570-575); a triage error propagates even though the chat persisted. Drag-drop (ingestCaptureFiles 586-596) returns {chatId:'', error} and the toast counts it as 'failed' (App.tsx:408-417); the watcher path (664-671) logs 'Failed to capture …' and omits the capture from capture:ingested so IN doesn't refresh; the tickler path (ingestTicklerFile 460-474) propagates before deleteTicklerFile, leaving the parked file alongside the new chat, and the renderer's fileToIN (App.tsx:1158) has no catch so nothing visibly happens. In every case the chat exists, so retrying (re-drop, 'to IN' again, next date sweep) creates duplicate chats.

**Repro:** User drags a photo in while the API is rate-limited: transcription succeeds, chat is created, triage throws. Toast says '1 failed', so they drop the file again — two duplicate capture chats in IN. Clicking 'to IN' on a parked file during a network blip likewise duplicates it on retry while the file finally leaves the tickler.

### src/main/index.ts:877 — One persistently failing chat aborts triage:all-changed midway — later chats never get triaged and housekeeping never runs

Inside triageAllChanged the per-chat `await triageChat(c.id)` has no try/catch (unlike refreshTriageMemory and proposeForProject in the same function). A chat whose extraction consistently errors (e.g. huge captured text → API 400 every run) rejects the whole handler; chats after it in table order are never triaged, and autoOrphanStale/auto-delete/goal proposals (884-900) are skipped — on every subsequent run, since the failing chat's hash never updates.

**Repro:** A very large captured text makes extractTriage fail every time. Every 'Triage changed chats' run shows the misleading 'check your API key' error, every chat created after that one is silently never triaged, and stale-project housekeeping and goal proposals stop running entirely.

### src/main/index.ts:208 — Triaged-hash recorded from post-extraction content — messages sent during a triage run are never triaged

triageChat snapshots messages at line 159, awaits the model call (line 167), then records setChatTriagedHash(chatId, chatContentHash(chatId)) at line 208 (also 181 on the park path) — recomputing the hash from the DB at COMPLETION time rather than hashing the extracted snapshot. Any message added during the await is folded into the recorded hash without being seen by the extractor; the batch scan (line 876) then permanently skips that content.

**Repro:** Batch triage runs while the user keeps chatting; 'ok, I'll call the dentist tomorrow' lands during the in-flight extraction. It is never extracted, but the chat is marked triaged at the new hash — no call item is ever suggested until the chat happens to change again.

### src/main/index.ts:425 — Parking a file with a name that already exists on that tickler date silently overwrites the earlier file

saveTicklerFiles writes with writeFileSync(join(dir, safeFileName(name))) — no uniqueness suffix or existence check, unlike the capture path's Date.now() prefix (line 657). A second file with the same (or sanitized-to-same) name parked on the same date destroys the first's content with no warning, and the returned count still reports it as saved.

**Repro:** User parks phone photo 'IMG_0001.jpg' on March 5, later parks a different 'IMG_0001.jpg' (default camera naming) on the same date. The first file is silently replaced; on March 5 only the second exists.

### src/main/index.ts:421 — saveTicklerFiles mkdirSync is uncaught — parking files fails with an unhandled rejection and zero user feedback when the capture folder is unavailable

mkdirSync(dir, { recursive: true }) at line 421 is outside any try/catch. If captureFolder points at a disconnected drive or read-only location it throws; the 'tickler:save-files' IPC rejects; the renderer's addDayFiles (App.tsx:1150-1157) awaits without a catch, so the rejection is unhandled — no message, files not parked.

**Repro:** User's capture folder is on a currently disconnected USB/synced drive. They drag files onto a Nudge Queue day: nothing happens — no confirmation, no error — and the files were never parked.

### src/main/index.ts:878 — Deleting a chat during a triage run resurrects it as ghost suggestions bound to the deleted chat

triageAllChanged snapshots allChatsForTriage() then awaits triageChat per chat (874-881). If the user deletes a queued chat mid-run, the later triageChat still executes: getChat (store.ts:989-991) has no deleted_at filter, and replaceChatSuggestions inserts fresh 'suggested' items with the deleted chat's id and project. deleteChat's item-unbinding ran before these inserts, so the new items dangle on a soft-deleted chat and appear in the IN/list views.

**Repro:** User starts 'Triage changed chats' (minutes over many chats), then deletes an old chat from the sidebar while it runs. After the run, suggestions from the deleted chat appear in IN; their source-chat link points at a chat that exists nowhere in the UI.

### src/main/index.ts:852 — refreshTriageMemory saves a pre-await settings snapshot, reverting any settings the user saved during the model call

refreshTriageMemory (846-854) snapshots `const s = getSettings()`, awaits generateTriageMemory (a multi-second network call), then persists saveSettings({ ...s, triageMemory, triageMemoryAt }) — rewriting EVERY field from the pre-call snapshot. Any settings:set landing during the await (Settings save, composer model/effort/thinking change) is fully overwritten seconds later. It runs at the start of every triageAllChanged (line 870), exactly when the user is likely interacting while triage grinds.

**Repro:** User clicks 'Triage changed chats', then while it runs points the watched capture folder at a new directory in Settings and saves. Seconds later the memory call completes and rewrites settings.json from its stale snapshot — the new capture folder silently reverts and their phone notes stop being ingested.

### src/main/store.ts:365 — Re-triage dedup matches decided items by exact kind+title, so renamed or list-moved items come back as duplicates

replaceChatSuggestions skips a fresh draft only when `${kind} ${title}` exactly matches a non-suggested row of the chat. Renaming a confirmed item or moving it to another kind breaks the match, so the next re-triage (auto-triggered by completing any sibling, or 'Triage Anew') re-inserts the model's original extraction as a duplicate suggestion alongside the corrected item. The placement-aware `existing` list sent to the model mitigates only probabilistically.

**Repro:** User confirms 'Call dentist — (555) 123-4567', renames it to 'Book cleaning w/ Dr. Ames', then marks another item from the chat done. The re-triage re-extracts the original title and it reappears in IN as a duplicate.

### src/main/store.ts:320 — 'waiting' items don't count as open actions for goal-stall logic — redundant proposals and premature Reference-archiving

projectNeedsGoalAction (320) and projectsNeedingGoalAction (301) count only status IN ('suggested','active') as open, excluding 'waiting' — inconsistent with autoOrphanStale (863). Downstream, proposeForProject (index.ts:788) fetches statuses ['suggested','active','done','dismissed'] so waiting items are invisible to the model, and hasOpenAction (index.ts:802-805) also excludes waiting, so goalMet can archive the project to Reference while a waiting item is outstanding.

**Repro:** A goal project's only remaining action is marked Waiting ('sent the contract, waiting on Sample'). The next batch triage treats the project as stalled: the model proposes a duplicate step, or judges the goal met and silently archives the project to Reference while the user is still waiting.

### src/main/store.ts:460 — assignChatItemsToProject matches next actions by belongs_to NAME globally across ALL chats, stealing items from unrelated chats

The first UPDATE (459-462) is WHERE kind='next' AND belongs_to = ? with no chat_id scope (the buy/call clause below IS chat-scoped). belongs_to holds a free-text candidate name from the triager, so two chats each producing a generically named candidate ('Taxes', 'Move') tag actions with the same string. Confirming the candidate from one chat re-projects every matching next action database-wide, including ones filed under other projects — splitting them from their source chats, violating the item↔chat unity invariant moveChat protects; a later move of the foreign chat silently drags them back out.

**Repro:** Chat A (user's taxes) and chat B (a relative's taxes, in another project) both yield a 'Taxes' candidate with tagged next actions. Making a project from chat A's candidate silently moves chat B's actions into it too; moving chat B later yanks them back out.

### src/main/store.ts:742 — Parking a chat destroys the original project filing of its confirmed/done items — resurfacing delivers everything to IN unfiled

parkChat (742-746) reassigns the chat AND all its items (including confirmed active and done ones, via moveChat) to the tickler folder, recording nothing about the original project. On the due date triageTicklerDate does moveChat(c.id, null), delivering everything to IN: previously confirmed and completed items permanently lose their project assignment (done items vanish from the project's history) with no automatic restore.

**Repro:** User files a chat under 'Kitchen Reno', confirms two actions and completes one, then parks the chat for next month. On triage of the due date, the chat, both actions and the done item land in IN unfiled — the Kitchen Reno placement is silently gone.

### src/main/store.ts:250 — claimItemChat can bind an item to a chat in a different project, creating the split state the store elsewhere forbids

claimItemChat (249-252) sets chat_id without reconciling projects. Dropping a chat living in project B (or IN) onto a chatless item in project A leaves the item in A and the chat in B — the 'items must never split across projects' state moveChat's comment (129-135) calls a tracking nightmare. From then on moving the chat silently relocates the item and vice versa.

**Repro:** User drags a chat from IN onto a chatless item under 'Client Sample' to claim it. Later they file that chat under a different project — the Sample item silently moves with it and disappears from the Sample project page.

### src/renderer/src/App.tsx:969 — Renderer writes full stale AppSettings snapshots (composer toggles and Settings Save), silently reverting fields main wrote — learned triage memory and lastListExportDay are wiped

App.tsx fetches settings once at mount (line 316-319) and never re-fetches. Main mutates settings.json behind its back: refreshTriageMemory saves freshly-learned triageMemory/triageMemoryAt on every 'Triage changed chats' run (index.ts:852) and maybeExportPlanLists saves lastListExportDay (index.ts:772). changeModel/changeEffort/changeThinking (App.tsx:968-984) then send the FULL stale snapshot ({ ...settings, chatModel: m }) through settings:set, and saveSettings (settings.ts:61-86) persists every field — overwriting the learned memory with the mount-time value. Settings.tsx save() (190-211) has the same problem: it seeds triageMemory/triageMemoryAt local state from the stale prop at mount (72-73) and writes them back wholesale. triageMemoryAt also rolls back, shifting the correction-learning window.

**Repro:** User rejects items with reasons, runs 'Triage changed chats' (memory is distilled and saved), then merely switches the chat model in the composer dropdown (or saves an unrelated Settings change). The stale snapshot is written back: the just-learned triage preferences are silently erased and future triages repeat the corrected mistakes.

### src/renderer/src/App.tsx:1832 — ChatRow/ChatList are defined inside App — every render remounts all chat rows, making clicks unreliable during streaming

ChatRow (1832) and ChatList (1876) are declared inside App, so their component identity changes every App render and React unmounts/remounts every chat row each time. App re-renders on every streaming delta of ANY chat. A click whose mousedown/mouseup straddle a remount never fires — the exact failure mode the codebase documents and fixed at ItemRow (3152-3155) by hoisting to module level.

**Repro:** While any chat is generating, clicks on a chat row's stop button, title, or ⋯ menu items intermittently do nothing (rows remount many times per second) — 'Stop generating' is flaky exactly when needed.

### src/renderer/src/App.tsx:1421 — Deleting an ancestor project while viewing its descendant strands the user on a broken placeholder page

removeProject (1419-1428) only navigates away when the deleted project IS the currently viewed one. store.deleteProject soft-deletes the entire subtree (store.ts:911-920), so deleting an ANCESTOR of the open project makes byId.get(projectId) undefined and the main pane's `section === 'project' && project` branch fails, rendering the dead-end fallback 'This view arrives in a later phase. For now it's a placeholder.' (3091-3096).

**Repro:** User opens 'Kitchen reno / Cabinets', then clicks × on 'Kitchen reno' in the sidebar. The main pane shows an empty heading and the placeholder text — a nonsense screen — until they click something else.

### src/renderer/src/App.tsx:3671 — Deadline/Price/When/Location sorts inherit the default descending direction, putting missing-value sentinel items first and most-urgent items last

sortAsc defaults to false (245) and persists across sort-key changes. Deadline uses sentinel '9999-99-99' (3671), price +Infinity (3639), location/when U+FFFF (3626, 3632) — all designed for ascending order. With the default descending direction the sentinels sort FIRST: 'Time-sensitive' shows undated items on top and the nearest deadline last; 'Price' shows unknown-cost items above the most expensive.

**Repro:** User opens Things to buy and selects sort 'Price' (direction at default ↓): no-cost items appear at top and the ordering is inverted; on Next actions 'Time-sensitive' puts the item due tomorrow last.

### src/renderer/src/useChat.ts:136 — A rejected chat:send / chat:retry invoke permanently wedges the chat in the busy/streaming state

send() adds the chat to busyChats and appends a streaming placeholder before `await window.api.sendMessage(...)` (135-137); busyChats is only cleared by the chat:done event. If the invoke rejects before streamAssistant's try/catch — main's addMessage/persist writeFileSync throwing (DB file locked by AV/sync, disk full), a throw in the pre-try section (index.ts:1062-1096), or the catch-path addMessage failing — no chat:done is ever sent. The Composer discards send()'s promise (App.tsx:3926), so it is also an unhandled rejection; retry() (162) has the identical hole.

**Repro:** User sends a message while OneDrive/antivirus briefly locks sample.sqlite: the invoke rejects; the chat shows an empty streaming placeholder forever, Stop does nothing (no AbortController registered), and the busy gate blocks all further messages to that chat until restart.

### src/preload/index.ts:21 — triage:run return drift: main returns `parked` but preload/renderer ignore it — user sees 'No items found' while their chat is silently parked

triageChat returns { counts, total, added, parked } and can auto-park the whole chat (index.ts:178-189, counts all zero, parked:1). The preload type for runTriage declares only { counts, total }, and doTriage (App.tsx:643-666) reads only counts: it prints 'No items found in this chat.' and never calls refreshChats/reloadInChats/setTicklerTick — unlike doTriageAll, which handles res.parked (686-691).

**Repro:** User triages a chat containing 'tickets go on sale March 3': main parks it into a Nudge Queue folder, but the UI reports 'No items found in this chat.' and the chat still shows in its old list, later vanishing with no explanation.

### src/renderer/src/App.tsx:651 — Triage summaries omit the event count: a chat that yields only events reports 'No items found in this chat.'

doTriage builds its summary from c.next, c.buy, c.call, c.project, c.inbox (651-657) but main's counts also include `event` (index.ts:210). If only events were extracted, the message falls through to 'No items found in this chat.' The same omission is in retriageProject's `found` sum (1596), which reports 'nothing new' even when events surfaced.

**Repro:** User pastes a dentist-appointment confirmation and clicks Triage: an event item is correctly created (visible under Events), but the status line says no items were found, so the user re-runs triage or files it by hand.

### src/renderer/src/Settings.tsx:526 — Choosing 'Custom…' capture interval never syncs captureIntervalMs to the displayed minutes

The interval dropdown's onChange (523-531) on 'custom' only sets captureCustom=true and pre-fills the minutes textbox — it never calls setCaptureIntervalMs; the value only updates when the user types a >0 number (547-551). So the saved value can differ from the display: coming from a sentinel like 'On app close' (-1) the box shows 0.5 minutes but Save persists -1; toggling custom→preset→custom shows old custom minutes while the preset's ms is saved; typing 0 or clearing keeps the previous value.

**Repro:** User's interval is 'On app close'. They pick 'Custom…' (box shows 0.5) and Save without touching the number. They believe polling is every 30s, but -1 was saved: the folder is never polled during the session, and reopening Settings shows 'On app close' again.

### src/renderer/src/Settings.tsx:140 — addConnector generates ids from list length, producing duplicate connector ids after add/remove sequences

addConnector (137-142) assigns id `connector-${prev.length + 1}` with no uniqueness check, so add/remove sequences collide (add two → connector-2/3, remove -2, add → second connector-3). The id is the MCP server key (buildMcpConfig, connectors.ts:79 does mcpServers[c.id] = ...), so one duplicate silently replaces the other and is never launched, while both connectors' tool allow/ask/deny entries land under the same mcp__connector-3__ namespace and cross-apply to whichever server survived.

**Repro:** User adds two custom MCP connectors, deletes the first, adds another, enables both. In an Agent SDK chat only one server actually starts, and a Deny set on a tool of the dropped connector can block the identically-named tool of the one that ran.

### src/renderer/src/Settings.tsx:164 — 'Update from my corrections' discards unsaved hand edits to the triage-memory textarea

refreshMem (160-170) calls updateTriageMemory(), and main reads the last-SAVED triageMemory from settings.json (index.ts:847-851) — never the textarea's unsaved content. The result unconditionally overwrites local state via setTriageMemory(mem) (164); with no new corrections, main returns the old saved text verbatim, so unsaved edits are simply replaced. The hint text explicitly invites editing by hand AND updating, making this sequence natural.

**Repro:** User hand-edits the 'Triage memory (learned)' textarea, then clicks 'Update from my corrections' before Save. The textarea reverts to the previously saved memory and everything typed is gone with no undo.

### src/main/settings.ts:33 — Corrupt settings.json silently resets all settings, and the next save makes the reset permanent

getSettings swallows any read/parse failure and returns pure defaults with no warning and no preservation of the broken file. Because every save writes the full merged object (line 84, itself a non-atomic writeFileSync — a plausible source of the truncation), the first subsequent save (even switching chat model) permanently replaces the user's captureFolder, triageRules, learned triageMemory, connectors, and custom commands with defaults. Until then, background consumers quietly run against defaults, so capture watching just stops.

**Repro:** settings.json is truncated by a crash mid-save. Next launch everything looks fresh — greeting reset, watched folder off, slash-commands gone. The user flips the model picker, and their real settings file is overwritten for good with no error at any point.

### src/shared/effort.ts:15 — Effort "xhigh" (Extra) is sent to Sonnet 4.6 / Opus 4.6, which only accept low/medium/high/max — chat 400s on the direct-API engine

supportsEffort() gates only on !model.includes('haiku'), and EFFORT_LEVELS offers xhigh ('Extra') for every non-Haiku model, so streamChat (anthropic.ts:320) forwards output_config:{effort:'xhigh'} to models that predate xhigh (introduced with Opus 4.7). Both Sonnet 4.6 and Opus 4.6 are in the picker (models.ts:15-16); the API rejects the unsupported level with a 400. The Agent SDK engine silently downgrades per its docs, so only the default API engine breaks.

**Repro:** User on the API engine sets effort to 'Extra' and picks Sonnet 4.6 (or Opus 4.6). Every message errors with '⚠️ Couldn't reach Claude: 400 …' instead of streaming; the combination looks valid in the UI so there is no clue why.

### src/provider/anthropic.ts:437 — transcribeCapture caps output at 2048 tokens and never checks stop_reason — dense captures are silently truncated

transcribeCapture sets max_tokens: 2048 and returns the joined text blocks (434-453) without checking resp.stop_reason === 'max_tokens'. A dense capture (full page of handwritten notes, long screenshot) can exceed 2048 output tokens since the prompt demands VERBATIM transcription; the truncated prefix is treated as complete, becomes the capture chat's first message and the sole triage input, so tasks in the lost tail never become items — the exact data-loss class this path was built to prevent (per its own comment about the Phase 0 truncation).

**Repro:** User photographs a dense two-column todo sheet and drops it into the capture folder. The capture chat ends mid-list; the remaining tasks never appear in IN and nothing warns the transcription was cut off.

### src/shared/ics.ts:60 — Events starting 23:01-23:59 with no end time (and cross-midnight end times) get DTEND earlier than DTSTART

addOneHour wraps the hour with (h + 1) % 24 but eventSpan (66-68) keeps the same calendar date for the end, so a 23:30 start with no stated end yields DTSTART ...T233000 / DTEND ...T003000 on the SAME day — an end 23 hours before the start. The same invalid span results when the model supplies an endTime past midnight ('9 PM to 1 AM'), which the schema invites. RFC 5545 requires DTEND after DTSTART; Google/Apple Calendar reject or mangle such events, and the pre-filled Google Calendar form errors on Save.

**Repro:** User triages 'concert Friday at 11:30 PM' → startTime 23:30, no end. The exported .ics event is rejected on Google Calendar import (negative duration) while 10 PM events import fine.

## Confirmed — low

### src/main/index.ts:653 — Files with future mtimes (clock-skewed phone/cloud sync) are skipped by the settle guard indefinitely

The stability check `Date.now() - st.mtimeMs < 4000` treats any file whose mtime is in the future (negative delta) as still-being-written and skips it every poll until wall-clock time passes mtime+4s. Sync tools preserve the source device's mtime, so a phone with a fast clock produces files skipped for hours with no log or indication.

**Repro:** User's phone clock is 2-3 hours fast; a synced photo note lands in the watched folder. The 30s poll silently skips it for hours before it is finally captured, while the user assumes capture is broken.

### src/main/index.ts:472 — Variant day folders for the same date resolve first-match only: duplicate filenames are re-ingested into duplicate chats and deletes/opens hit the wrong copy

The tickler tolerates multiple folder spellings for one date ('07 July/09' and 'July/9'); listTicklerFiles unions names across them (341-351) but ticklerFilePath (333-339) resolves only the FIRST match. ingestTicklerFile/deleteTicklerFile therefore ingest+delete just one copy: the copy in the second folder survives, stays listed, and is re-ingested into a fresh chat on every subsequent 'Triage this date'; delete appears not to work; openTicklerFile may open a different file than the one listed.

**Repro:** 'note.txt' exists in both a hand-made 'July/9' and the app's '07 July/09'. Each 'Triage this date' click creates another duplicate 'note.txt' chat in IN, and deleting the listed 'note.txt' leaves it still showing — the date never fully clears.

### src/main/index.ts:1004 — Just-configured capture folder is never scanned in non-recurring modes if a poll is already in flight

settings:set fires 'void pollCaptureFolder()' to scan a newly configured folder promptly, but pollCaptureFolder returns immediately at line 628 if capturePolling is true (e.g. the launch catch-up scan is still transcribing). The skipped call is not queued or retried, and with captureIntervalMs <= 0 (on-open/on-close modes) no recurring timer exists (line 703), so the new folder is not scanned until the next launch.

**Repro:** Capture mode is 'on app open'. While the slow launch scan is still ingesting, the user changes the watch folder in Settings and drops files there. Nothing is captured for the rest of the session; the files are only picked up after a restart.

### src/main/index.ts:656 — Uncaught mkdirSync in the poll loop aborts the entire scan every tick if _captured cannot be created

mkdirSync(doneDir, { recursive: true }) at line 656 is inside the per-file loop but outside any try/catch. If _captured exists as a FILE (e.g. sync conflict) or creation fails on permissions, it throws, aborting the whole poll (remaining files skipped) with only an unhandled-rejection warning. The condition persists, so every tick fails: no files are ever captured and no error is shown.

**Repro:** A file literally named '_captured' lands in the watch folder. From then on every poll silently aborts; notes accumulate uncaptured with no error surfaced anywhere.

### src/main/index.ts:1126 — Closing the window during a streaming reply (with on-close capture mode) persists a garbage '⚠️ Object has been destroyed' message into the chat

In on-close capture modes, before-quit's preventDefault (1448) keeps the process alive up to 15s after the window is destroyed. An in-flight streamAssistant then throws from event.sender.send on the destroyed webContents; the catch at 1124-1127 persists an assistant message '⚠️ Couldn't reach Claude: Object has been destroyed' into the DB, replacing the retryable state, and the subsequent chat:done send in the catch throws again, rejecting the handler.

**Repro:** Capture mode is 'on close'. User sends a message and closes the app before the reply finishes. On next launch the chat ends with the nonsense error instead of either the reply or a clean retry point.

### src/main/index.ts:1267 — Activity-log digest feeds the model raw UTC timestamps as if they were local times

triage:digest formats each entry as e.createdAt.slice(0,16).replace('T',' ') — UTC wall time with no conversion or marker. For a US user everything is 4-5 hours late and any activity after ~7-8 PM local carries the next day's date, so the digest narrates work on the wrong day.

**Repro:** User triages Tuesday 9-11 PM and generates the Daily digest Wednesday morning: it describes the work as happening 'Wednesday between 1 AM and 3 AM', misattributing the whole evening.

### src/main/index.ts:260 — Feb 29 (and other impossible month/day combos) roll to a nonexistent ISO date instead of the next real occurrence

nextOccurrenceISO builds `${year}-${mm}-${dd}` with no calendar validation: a '02 February/29' folder evaluated after Feb 29 maps to e.g. '2027-02-29', which doesn't exist. The renderer month grid offers Feb 29 every year (App.tsx:34), so parkChat can create a DB tickler folder with tickler_due='2027-02-29'; the phantom date only becomes due lexicographically on Mar 1 2027, and formatEventDate silently rolls it over.

**Repro:** In July 2026 the user parks an anniversary chat on February 29. It files under 2027-02-29; the nag fires March 1 2027 (not the actual leap day 2028) and date formatting displays March 1.

### src/main/store.ts:872 — autoOrphanStale retires only the root project — active subprojects with goals keep generating proposals for the retired tree

The UPDATE at 872 sets location on root.id only; subprojects keep location='active'. activeGoalProjects (310-312) and projectNeedsGoalAction (318) read p.location directly, so goal-bearing subprojects of a DORMANT/Reference root are still treated active: the batch goal proposer keeps creating suggested items for them (which appear in the smart lists), and proposeForProject can independently flip just a subproject to 'reference'.

**Repro:** A project tree with a goal-bearing subproject idles for a year and is auto-orphaned to DORMANT. Model proposals for the subproject keep appearing in the Next list indefinitely, for a tree the app itself declared abandoned.

### src/main/store.ts:990 — getChat ignores soft-deletes and purgeProject leaves dangling chat_id on outside items — dead/ghost 'Open source chat' links and messages excluded from triage

getChat (989-991) has no deleted_at IS NULL filter, and purgeProject (948-961) hard-deletes chats without clearing chat_id on items living in OTHER projects (splits created by assignChatItemsToProject/claimItemChat) — unlike soft deleteChat, which does clear it. Surviving items keep '↗ Open source chat': it opens a ghost chat that appears in no list and that allChatsForTriage never scans (messages typed there are never triaged), or after purge a blank nonexistent chat; completing such an item re-triages a zero-message conversation.

**Repro:** make-project moves next actions into a new project; the user later purges the source chat's old project. Clicking 'Open source chat' on a surviving item opens a chat that exists nowhere in the sidebar; anything said in it is silently excluded from all future triage.

### src/main/store.ts:186 — recentMessages feeds messages from deleted chats into memory distillation

recentMessages (184-189) selects from messages with no filter against chats.deleted_at, unlike searchMessages (200). Messages of soft-deleted chats continue to shape the distilled memory profile (index.ts refreshMemory, line 124).

**Repro:** User deletes a chat precisely because it was noise or unwanted influence. The next 24h memory refresh still distills preferences from that deleted conversation's messages.

### src/renderer/src/App.tsx:1087 — Calendar matches tickler dates by month/day only — an overdue folder shadows the same day next year

ticklerDateFor (1085-1090) finds the first TicklerDate whose month and day match, ignoring year; the month grid uses `date ? date.iso : nextOccurrenceISO(...)` as both open and park target (1223). listTicklerDates is sorted ascending, so an overdue this-year folder always wins over a next-year folder for the same day: the next-year folder becomes unreachable from the calendar, and chats dropped on that day are parked into the overdue (immediately due) folder.

**Repro:** User has an untriaged overdue March 1 folder plus a birthday chat parked for next March 1. Clicking March 1 only ever opens the overdue folder, and dragging a new chat onto March 1 files it where it nags immediately instead of next March.

### src/renderer/src/App.tsx:626 — Digest generation (and date-triage) failure is an unhandled rejection with zero feedback

regenerateDigest (621-631) has try/finally but no catch: if the triage:digest IPC rejects (bad/missing API key, network error) the rejection is unhandled, digestBusy resets, and the UI just shows 'No <period> digest yet.' — unlike other model flows which show 'check your API key in Settings'. doTriageDate (1115-1131) has the same try/finally-without-catch shape.

**Repro:** User with an invalid API key clicks Digest → Weekly → Generate. 'Writing the weekly digest…' flashes, then reverts to 'No weekly digest yet.' with no explanation, no matter how many retries.

### src/renderer/src/App.tsx:3064 — Digest 'Generate' gives no feedback when the selected period's log window is empty

regenerateDigest only caches when the IPC returns non-empty text; main returns '' when readTriageLogSince finds no entries (index.ts:1264). The UI then still shows 'No daily digest yet.' with the same Generate button and no explanation that there is nothing to digest.

**Repro:** User with a freshly cleared log clicks Digest → Daily → Generate; the busy text flashes and the view returns to 'No daily digest yet.' — repeated clicks appear broken rather than explaining the log is empty.

### src/renderer/src/App.tsx:1163 — '→ IN' on a parked file always shows a success toast, even when ingestion returned null or failed

fileToIN (1158-1165) ignores the CaptureResult returned by ticklerFileToIN — which can be null (file gone from disk) or carry an error — and unconditionally shows '"<name>" captured to IN.'; if the IPC rejects, the void'ed promise leaves an unhandled rejection and no toast at all.

**Repro:** User clicks '→ IN' on a parked file that was removed on disk. The toast cheerfully reports it was captured, but no chat appears in IN — the user believes the content was ingested when it was not.

### src/renderer/src/App.tsx:436 — Watched-folder captures that all fail produce no notification at all

onCaptureIngested (433-447) filters to successful results and returns early when none succeeded (`if (!ok.length) return;`). Failed results are never surfaced in the UI, and failure counts aren't included in the toast when some succeed.

**Repro:** User drops an unsupported/corrupt file into the watched folder. The watcher moves it out of the inbox folder, but the app shows nothing — no toast, no error — so the user assumes it was captured and never sees it in IN.

### src/renderer/src/App.tsx:1760 — Deleting a chat doesn't refresh item lists — bound items keep a dead 'Open source chat' link

deleteChatThen (1760-1766) refreshes the chat lists but never calls loadItemsFor, even though store.deleteChat unbinds every item (chat_id='', store.ts:157-162). On-screen items keep their stale chatId until an unrelated reload.

**Repro:** User deletes a chat while its extracted items are visible in a list. The items still show '↗ Open source chat'; clicking it opens a ghost view of the deleted chat instead of reflecting the store's new state.

### src/renderer/src/App.tsx:2167 — Goal-stall banner ignores proposed items hidden by the Proposed toggle

The 'Nothing's in motion toward this goal' banner checks projItems for suggested/active items (2166-2167), but loadProjItems only includes 'suggested' when showProposed is on (502-505). With the Proposed pill off, a project whose only open work is suggested items is misreported as stalled.

**Repro:** User turns off the Proposed pill on a project with several model-proposed next actions and none confirmed. The banner claims nothing is in motion and 'Suggest a step' stacks another proposal on the hidden ones.

### src/renderer/src/App.tsx:3869 — Thumbs up/down clicked on the still-streaming reply is silently dropped

Bubble renders feedback/retry/copy actions whenever message.content is non-empty (3852), including the live placeholder whose id is the '__streaming__' sentinel. onFeedback calls the IPC with that sentinel id; the UPDATE matches no row, nothing is stored or shown, and the click is not re-applied when the real id arrives. Retry on the live bubble is likewise a silent no-op.

**Repro:** During a long streaming reply the user clicks 👍. The thumb never lights and no feedback is recorded; they must click again after the stream ends.

### src/renderer/src/App.tsx:3638 — itemPrice mangles cost ranges and multi-number text — '$20-30' sorts as 2030

itemPrice strips every character except digits and '.' before parseFloat, so ranges and prose collapse into one bogus number: '$20-30' → 2030, 'about $30 each, need 3' → 303. The schema only asks the model for 'approximate cost', so range strings are normal data; Price sort then places a $20-30 item above $500 items.

**Repro:** User sorts Things to Buy by Price. A '$40–50' pair of shoes lands below a '$400' appliance because it parsed as $4,050.

### src/renderer/src/App.tsx:3647 — Sort-by-recipient uses the stale `who` baked into item data, ignoring renames and kind moves

itemRecipient reads `who` from the original triage-time data blob, never updated by updateItem. A renamed call still sorts under the extraction-time recipient, and an item moved into Phone calls (no `who` in data) falls back to its full title, interleaving incorrectly with rows sorted by bare names.

**Repro:** User renames 'Call Mom — (555) 111-2222' to 'Call the landlord about the lease', then sorts Phone calls by Recipient. The row still sorts under M for Mom.

### src/renderer/src/App.tsx:3982 — Escape in the composer erases the entire draft when the text starts with '/'

The keydown handler (3981-3984) treats any value starting with '/' as a command query and maps Escape to setValue('') — wiping the whole textarea rather than just dismissing the command popup.

**Repro:** User types a literal message beginning with '/' (e.g. '/etc/hosts keeps getting reset, help'); pressing Escape to dismiss the command menu deletes everything typed.

### src/renderer/src/App.tsx:3712 — parseAsk matches '```ask' inside an ordinary code block, hijacking the whole message into a choice card

parseAsk searches raw content for the first '```ask\n' (3713) without checking whether it sits inside another fenced block, then treats the next '```' as the closer. A reply whose code block demonstrates the ask syntax is sliced into before/question/options, rendering a bogus interactive card and mangling the surrounding markdown.

**Repro:** User asks 'show me the ask-block syntax you use'; the reply renders as a clickable question card with the example's bullets as options, and clicking one sends it as a message.

### src/renderer/src/App.tsx:3834 — Assistant messages from legacy databases (empty model column) are labeled with the currently selected model

Bubble computes the label as modelLabel(message.model || currentModel) (3834). Messages predating the model column (backfilled to '') display 'Claude · <currently selected model>', misattributing old replies; switching the picker retroactively relabels history.

**Repro:** User opens an old conversation while 'Opus 4.8' is selected — every historical reply claims Opus 4.8; flipping to Haiku relabels them all again.

### src/renderer/src/App.tsx:3274 — Saving a non-URL 'buy link' appears to do nothing — the chip only renders for parseable URLs

The buy chip renders only when buyUrlTarget(pins.buyUrl) is non-empty (3205, 3274), i.e. the text normalizes to an http(s) URL. 'Add buy link…' accepts any text and stores it, but afterwards no chip appears and there is no error; the only trace is the menu label flipping to 'Edit buy link…'.

**Repro:** User types 'Ace Hardware on 5th' into 'Add buy link…' and saves. The editor closes and nothing visible changes on the row — the user assumes the save failed and retries.

### src/renderer/src/App.tsx:3077 — Activity-log rows open chats with null project context (section IN) and can open ghost deleted chats

Log rows call openChatAt(e.chatId, null) because TriageLogEntry (store.ts:496-501) carries no projectId; openChatAt sets section 'in', so a chat filed in a project opens against the IN chat list where it doesn't appear — no breadcrumb, wrong sidebar context. For a since-soft-deleted source chat, the row still opens it (getChat has no deleted filter), letting the user unknowingly continue a deleted conversation.

**Repro:** User clicks a 'Triaged "Kitchen reno"' log row: the chat opens as if in IN with no breadcrumb and isn't in the sidebar list; clicking another chat loses it. A deleted chat's row opens a ghost conversation that exists nowhere else.

### src/renderer/src/App.tsx:458 — Concurrent 'ask' permission requests from two streaming chats clobber each other; the overwritten chat hangs forever

Two chats can stream concurrently, and each stream's onPermission (index.ts:1114-1119) registers a resolver in pendingPerms and sends chat:permission. The renderer holds a single permReq (457-471): a second request overwrites the first via setPermReq, so the first's resolver is never invoked and its streamChat awaits indefinitely — that chat stays busy until app restart (abort only kills the HTTP stream, not the pending canUseTool promise).

**Repro:** Two chats on the Agent SDK engine hit an Ask-configured connector tool at overlapping times: only one approval dialog appears; after answering it, the other chat's spinner never finishes.

### src/renderer/src/Settings.tsx:572 — 'Re-export all lists' ignores the folder typed in the box and shows a misleading failure message

The button (567-577) calls exportAllLists, and main's planListsDir() reads the SAVED captureFolder (index.ts:731-733), not the unsaved input value above it. The failure message is also hard-coded to 'Set a watched capture folder first.' for ANY ok:false result, including when a folder IS configured but the write failed (exportPlanList returns -1 on mkdir/write errors — e.g. disconnected drive).

**Repro:** User types a capture folder into the field, then clicks 'Re-export all lists' before Save: they're told to set a folder — the thing they just did — and nothing exports. A user whose saved folder is on an unmounted drive gets the same wrong message.

### src/renderer/src/Settings.tsx:192 — Clearing 'Your name' is silently undone — preview says 'there' but the saved greeting resurrects the OS username

save() writes name: name.trim() (an emptied field saves ''), but getSettings (settings.ts:37-38) replaces any empty greeting.name with the OS username on every load. The live preview (resolveTokens, greeting.ts:73) renders an empty name as 'there', so preview and persisted behavior disagree: the first-run fallback also overrides a deliberate deletion.

**Repro:** User deletes their name expecting 'Happy Tuesday, there' (as previewed), clicks Save, and after the next settings load the greeting says 'Happy Tuesday, REDACTED-USER' — the removed name comes back.

### src/provider/anthropic.ts:335 — streamChat never handles stop_reason 'refusal' — a Fable 5 classifier refusal is saved as a silent empty reply

Fable 5 (in the picker) can decline with HTTP 200, stop_reason 'refusal', and empty content; other 4.x models can too. streamChat only accumulates 'text' events — on a pre-output refusal no text fires, finalMessage() resolves normally, and streamAssistant (index.ts:1121) persists an empty assistant message with no error; stop_reason is never inspected in the streaming path.

**Repro:** User on Fable 5 asks a security-tooling question that trips a classifier: the chat shows a completely empty assistant turn with no explanation, persisted to the DB; retry produces the same blank.

### src/provider/anthropic.ts:11 — ANTHROPIC_MODEL override in .env is silently ignored (read before dotenv loads)

DEFAULT_TRIAGE_MODEL is computed at module-evaluation time (process.env.ANTHROPIC_MODEL ?? 'claude-sonnet-5'). Main imports the provider module (index.ts:75) before its body runs loadEnv (index.ts:91), so an ANTHROPIC_MODEL entry in .env — the same file documented as home of the API key, which only works because the client is constructed lazily — is never seen; the advertised override does nothing unless set as an OS-level env var.

**Repro:** Developer adds ANTHROPIC_MODEL=claude-opus-4-8 to .env to test a different triage model. Triage continues on claude-sonnet-5 with no error or log.

### src/provider/anthropic.ts:198 — ANTHROPIC_MODEL set to a Fable/Mythos model 400s every triage and utility call (thinking disabled hardcoded)

All six non-chat call sites hardcode thinking:{type:'disabled'} regardless of model: extractTriage (198), proposeGoalAction (269), generateMemory (349), generateTriageMemory (373), generateLogDigest (396), transcribeCapture (436). DEFAULT_TRIAGE_MODEL honors ANTHROPIC_MODEL, but Fable/Mythos reject explicit disabled thinking with a 400 — the alwaysOnThinking() guard used by streamChat is never consulted here. Several paths swallow errors as best-effort (index.ts:872, 898), so auto-triage of captures silently stops.

**Repro:** User sets ANTHROPIC_MODEL=claude-fable-5 in the OS environment. Manual triage buttons show a 400 and dropped captures quietly stop producing suggested items because the catch blocks discard the failure.

### src/shared/ics.ts:38 — icsFold folds at UTF-16 code-unit boundaries — can split an emoji surrogate pair and exceeds 75 octets on non-ASCII lines

icsFold slices at fixed indices 75/74 in JS code units, but RFC 5545 folding is defined in octets (max 75) and must not split a multi-octet character. A line whose fold boundary lands inside an emoji is split into lone surrogates serialized as U+FFFD, and any non-ASCII text makes folded lines exceed 75 octets — a spec violation most but not all parsers tolerate.

**Repro:** An event title with emoji long enough to fold right on the emoji exports and imports with replacement characters '�' in the middle of the title.

## Disputed (one verifier confirmed, one refuted)

### [high] src/provider/claudeAgent.ts:138 — Agent engine sends thinking:{type:"disabled"} to Fable 5/Mythos, which the API rejects with 400

The thinking-off branch is `supportsAdaptiveThinking(model) ? {type:"disabled"} : undefined` — it omits the `!alwaysOnThinking(model)` guard the direct-API engine has (anthropic.ts:309-315, with an explanatory comment) and doesn't even import alwaysOnThinking (line 5). src/shared/effort.ts:29-33 documents the invariant: Fable 5 / Mythos reject both disabled and enabled thinking with a 400; the param must be omitted or adaptive. The SDK's query() options forward the thinking config to the API, so Fable/Mythos get {type:"disabled"} whenever thinkingEnabled is false.

**Repro:** User sets engine to 'Claude Agent SDK', picks Fable 5, and toggles extended thinking OFF. Every message fails with '⚠️ Couldn't reach Claude: 400 …thinking…' until they re-enable thinking or switch models.

**Why disputed:** The report reads the app code correctly but the claimed failure path is unreachable. Confirmed: src/provider/claudeAgent.ts:135-142 builds thinking={type:"disabled"} for any non-Haiku model when thinking is toggled off, without the !alwaysOnThinking guard that src/provider/anthropic.ts:309-312 has, and Fable 5 is selectable (src/renderer/src/models.ts:7). However, the Agent SDK does not send this config to the API — it translates it to the CLI flag `--thinking disabled` (node_modules/@anthropic-ai/claude-agent-sdk/sdk.mjs: thinking option -> thinkingConfig -> `V.push("--thinking","disabled")`), and the bundled Claude Code CLI (claude-agent-sdk-win32-x64/claude.exe) sanitizes the wire-level thinking param per model. Its request builder emits {type:"disabled"} only when `K.type==="disabled" && Rq()==="firstParty" && !I7 && HZq(O) && !WoH(O)`; WoH(model) is an always-on-thinking predicate that returns true for "claude-fable-5" explicitly (false for all Opus 4.x/Sonnet 4.x/Haiku models). For Fable 5 the guard fails, G_ stays undefined, and the thinking parameter is omitted from the API request entirely — the exact configuration the API requires for Fable (always-on adaptive runs; no 400). This is behaviorally identical to the "correctly guarded" anthropic.ts path, which also omits the param for Fable. Mythos 5 is not offered in the app's model picker at all. So the repro ("every message fails with 400 ...thinking...") does not occur; the missing guard is only a cosmetic inconsistency between the two engines with no user-visible effect.

### [medium] src/main/index.ts:926 — Due-tickler state is never re-evaluated at day rollover — no File-menu TNON entry or nag while the app stays open

buildMenu computes dueTicklerDates once at window creation; rebuildMenu runs only inside tickler IPC handlers (park, triage-date, file ops — lines 1174-1252), and the auto-park triage path never calls it. The renderer's dueTickler state similarly reloads only on [ticklerTick, section] changes (App.tsx:588-592). Nothing re-checks when midnight passes, so a date that becomes due while the app is running produces no File-menu 'Today Needs Occasional Nudging!' entry and no home/sidebar nag until restart, section navigation, or an unrelated tickler action.

**Repro:** User leaves the app running overnight. A chat parked for today is now due, but no nag appears anywhere; the reminder is missed even though the app was open all day.

**Why disputed:** The code mechanism is as described (buildMenu reads dueTicklerDates() once at index.ts:926/987; rebuildMenu only fires from tickler IPC handlers at index.ts:1174-1252; renderer dueTickler reloads only on [ticklerTick, section] at App.tsx:589-592; no midnight timer, focus handler, or interval re-checks). But the claimed user-visible failure — "no nag appears anywhere; the reminder is missed even though the app was open all day" — does not survive the repro walk-through. The effect at App.tsx:589-592 re-fetches the due set on EVERY section change, so the user's first navigation click in the morning (the core interaction of a SampleApp-style app) restores the always-visible sidebar TNON button (App.tsx:2573) and home nag (App.tsx:2786) within seconds of actually using the app. The comment at App.tsx:586-588 explicitly documents this as the intended design: "a date that rolled over while the app was open is caught the next time you navigate" — rollover-while-open was considered and refresh-on-navigation deliberately chosen. Nothing is lost: dueTicklerDates() filters iso <= today (index.ts:411-414), so the date stays due until handled and resurfaces on any navigation, park, triage, or restart. Both auto-park paths bump ticklerTick in the renderer (App.tsx:419, 691), so the primary nag surface is refreshed there too. The only genuinely unrecovered surface is the native File-menu TNON entry, which stays stale until a tickler action or restart — but it is a tertiary duplicate of the sidebar nag hidden inside the File menu, and its click handler re-fetches the due set anyway (App.tsx:327-331). The full claimed failure requires a user who keeps the app open and uses it all day without ever changing section — no realistic flow. Residual: a transient stale nag recovered on first interaction plus a stale redundant menu shortcut = low severity, and the renderer half is documented intentional behavior.

### [medium] src/renderer/src/App.tsx:3991 — Legacy settings.chatModel not in the current CHAT_MODELS lineup makes the picker display 'Fable 5' while sends use the old model id

getSettings (settings.ts:43) passes chatModel through unvalidated, and the model lineup was replaced (models.ts). The controlled <select> (3989-4001) has no option matching a retired id, so the browser displays the first option ('Fable 5') while settings.chatModel and every send still use the stale id; if that id is retired by the API, chats fail while the picker shows a valid model.

**Repro:** User upgrades from a build with chatModel 'claude-sonnet-4-5'. The composer picker shows 'Fable 5', but replies stream from (or error on) the old model; the per-message label contradicts the picker.

**Why disputed:** The mechanism is accurately described: getSettings (src/main/settings.ts:43) passes chatModel through unvalidated; the composer picker (src/renderer/src/App.tsx:3989-4001) is a controlled select over CHAT_MODELS only, and React's controlled-select fallback would display the first option ("Fable 5") without firing onChange, while sends (src/main/index.ts:1066/1106) and the per-message label (App.tsx:3834 via modelLabel's raw-id fallback) use the stale id. Nothing downstream snaps or heals the value. HOWEVER, the repro's precondition — a settings.json containing a chatModel id absent from the current lineup — is unsubstantiated: chatModel is only ever written by the picker (lineup ids) or saveSettings (preserve/default), so the bug requires an id that was once offered and later removed from CHAT_MODELS. All available evidence says that never happened: the lineup (models.ts:5-17) retains a deep tail of older models (opus-4-7/4-6, sonnet-4-6, haiku-4-5), every model id referenced anywhere in the repo (including the earliest spike scripts) is still in the lineup, the current default (claude-sonnet-5, greeting.ts:51) is in the lineup, and the report's cited legacy id "claude-sonnet-4-5" appears nowhere — the app's development began in the sonnet-4-6 era, so no shipped build ever wrote it. No realistic upgrade flow produces the claimed picker/send mismatch today; this is a latent robustness gap that only materializes if a future lineup edit drops a previously-offered id. Per the default-to-refuted standard for an unevidenced repro, refuted; graded low as a rare/currently-unreachable hazard.

### [medium] src/shared/ics.ts:53 — addOneDay throws RangeError on regex-valid but impossible dates (2026-06-31, non-leap Feb 29), killing the entire .ics export and the Google Calendar link

eventDetails validates dates only with /^\d{4}-\d{2}-\d{2}$/, which accepts impossible dates the model can emit (it resolves relative phrases itself; 'end of June' → 2026-06-31 is a plausible slip) and legacy data can carry. For an all-day event, eventSpan → addOneDay does new Date('2026-06-31T00:00:00Z') → Invalid Date, then .toISOString() throws RangeError. buildICS iterates ALL active events with no per-event guard, so one bad event aborts the whole calendar:export (after the user picks a save path; no file written); calendar:export-one and calendar:google → eventSpan fail the same way. The bad item looks normal in the UI because formatEventDate (schema.ts:210) silently rolls it over. eventSpan's HHMM regex similarly accepts 24:00/99:59, emitting invalid DTSTART/DTEND.

**Repro:** One confirmed all-day event carries date 2026-06-31. User clicks Events → 'Export .ics', picks a filename → the handler throws, no file is created, and no events (including valid ones) are exported; 'Add to Google Calendar' on that event silently does nothing.

**Why disputed:** The report's crash mechanism does not exist in V8. It hinges on new Date("2026-06-31T00:00:00Z") being an Invalid Date so that addOneDay (ics.ts:53-57) throws RangeError in .toISOString(), aborting buildICS and the calendar:export/export-one/google handlers (index.ts:1193/1200/1230). Verified against V8's own source: dateparser.h defines IsDay as Between(1,31) and IsMonth as Between(1,12) with no days-in-month or leap-year validation; ParseES5DateTime (dateparser-inl.h) validates day only via IsDay; and ParseDateTimeString feeds the components to MakeDay (date.cc), which is pure arithmetic rollover ("day_from_year - 1 + DoubleToInteger(date)"). So 2026-06-31 parses as 2026-07-01 and non-leap Feb 29 as Mar 1 — addOneDay returns "2026-07-02" without throwing, buildICS completes, the .ics file IS written, and the Google Calendar link opens. The claimed failure (export dies after the save dialog, no file, all events lost, silent Google-link no-op) is unreachable. The report correctly observes the weak regex validation (ics.ts:8-9, 16), the unguarded loop (ics.ts:76-94), and formatEventDate's silent rollover (schema.ts:210), but the actual consequence is only a residual low-severity defect: the export emits an RFC-invalid DTSTART;VALUE=DATE:20260631 (with DTEND rolled to 20260702) for that single malformed event, and HHMM accepting 24:00/99:59 emits an invalid timed DTSTART while the UI shows "All day" — wrong output for one bad event, not a crash or loss of the export.

### [low] src/main/settings.ts:12 — Connector migration normalizes only `tools` — a legacy/hand-edited connector missing args (or command) crashes the Connectors tab, Settings save, and agent chats

migrateConnectors (10-13) defaults tools via c.tools ?? [] but passes args/command through untouched, though settings.json is a human-editable file and tolerating old shapes is the function's purpose. A connector without args reaches the renderer: Settings.tsx:743 crashes rendering the tab (c.args.join), save() throws at line 197 (c.args.filter), and buildMcpConfig (connectors.ts:70/79) throws on c.command.trim or hands args: undefined to the Agent SDK — failing every agent-engine chat send.

**Repro:** User hand-adds a connector entry without an 'args' key to settings.json. Opening Settings → Connectors throws 'Cannot read properties of undefined (reading join)' and the pane goes blank, so they can't fix it from the UI; with engine=agent, chat sends error instead of streaming.

**Why disputed:** The code mechanism is accurately described: migrateConnectors (src/main/settings.ts:12) only normalizes tools, and an args-less connector would crash Settings.tsx:742 (c.args.join), Settings.tsx:197 (c.args.filter on save), and potentially connectors.ts:70 (c.command.trim). However, the repro requires the user to hand-craft a malformed connector entry in settings.json — a state no code path has ever produced. Every writer (DEFAULT_CONNECTORS, addConnector at Settings.tsx:140 which sets args: [], renderer save() at :197, saveSettings merge at settings.ts:68/84) always emits complete connectors, and the only legacy shape that ever existed (per-tool {name, allowed}, documented at connectors.ts:16) always carried connector-level command/args — so the audit's legacy-data angle produces no trigger and normalizeConnectorTool covers the one real migration. The app's UI fully manages connectors; nothing directs users to hand-edit the file. The failure class is unbounded garbage-in (args as string, connectors as object — the latter crashes migrateConnectors itself even earlier), not a specific defect; the report cherry-picks one omission among infinitely many malformed edits. The chat-side claim additionally requires the hand-edit to include enabled:true (connectors.ts:70 filters on c.enabled, so a typical partial entry is silently skipped), and args:undefined with a valid command is legal for spawn, making even that leg uncertain. No data loss or corruption occurs in any branch; the hand-edited file is recoverable by hand-editing again. Per the rubric — refute when no realistic user flow surfaces the failure — this is a hypothetical self-inflicted misconfiguration, not a real defect a user would hit.

## Refuted (verified not to be bugs)

- src/main/index.ts:942 — On macOS the 'File' menu template becomes the application menu, misplacing New Conversation/Settings/Exit
- src/main/index.ts:1429 — Daily plan-lists auto-export only triggers at app startup, never while the app stays running across days
- src/main/store.ts:926 — deletedProjects() omits the ticklerDue column its Project return type promises
- src/renderer/src/App.tsx:1376 — Chat search has no request sequencing — out-of-order responses show results for a stale query
- src/renderer/src/App.tsx:1051 — depthOf and crumbs have no cycle guard — a parent-loop in legacy project rows hangs the renderer
- src/provider/anthropic.ts:322 — Chat web search uses the legacy web_search_20250305 tool variant on models whose current variant is web_search_20260209
- src/provider/claudeAgent.ts:160 — permissionMode 'bypassPermissions' is used without allowDangerouslySkipPermissions, which the installed Agent SDK documents as required

